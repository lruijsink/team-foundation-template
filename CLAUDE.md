# team-foundation-template

Don't spend effort manually formatting `.kt`/`.kts`/`.md`/`.mdx` files - a Claude Code hook runs `format.sh`
automatically after every edit (when enabled via `git config hooks.claude-auto-format true`). Focus on content and
correctness; formatting is handled for you.
