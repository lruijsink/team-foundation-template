{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "if [ \"$(git config --type=bool --get hooks.claude.autoFormat)\" = \"true\" ]; then tools/linter.sh -w \"$(jq -r '.tool_input.file_path')\"; fi"
          }
        ]
      }
    ]
  }
}
